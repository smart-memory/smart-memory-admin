name: Deploy Admin to Hetzner

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: smart-memory/smart-memory-infra/.github/workflows/deploy-ui.yml@main
    with:
      config_path: deploy.config.yml
    secrets: inherit
